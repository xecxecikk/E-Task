//
//  CartItemEntity+Mapping.swift
//  E-Task
//
//  Created by XECE on 13.07.2025.
//

import Foundation

extension CartItemEntity {
    /*func toCartItem() -> CartItem {
     let product = Product(
     id: String(self.id),
     name: self.name ?? "",
     imageURL: self.imageURL ?? "",
     price: String(self.price),
     details: self.details ?? "",
     model: self.model ?? "",
     brand: self.brand ?? "",
     isFavorite: self.isFavorite
     )
     return CartItem(product: product, quantity: Int(self.quantity))
     }
     }
     
     func updateCartItemEntity(_ entity: CartItemEntity, from cartItem: CartItem) {
     entity.id = Int64(cartItem.product.id)!
     entity.name = cartItem.product.name
     entity.imageURL = cartItem.product.imageURL
     entity.price = Double(cartItem.product.price)!
     entity.details = cartItem.product.details
     entity.model = cartItem.product.model
     entity.brand = cartItem.product.brand
     entity.isFavorite = cartItem.product.isFavorite
     entity.quantity = Int64(cartItem.quantity) */
}
